# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Timed Mastery' do
  use_frameworks!
  pod 'Charts'
  pod 'RealmSwift'
  target 'Timed MasteryTests' do
    inherit! :search_paths
  end

  target 'Timed MasteryUITests' do
    inherit! :search_paths
  end

end
